<div class="products floatLeft">
	<h1><%= @title %></h1>
	<ul>
		<% @products.each do |product| %>
			<li>
				<%= link_to product.title, product_path(product) %>
				<label><%= number_to_currency product.price %></label>
			</li>
		<% end %>
	</ul>
</div>